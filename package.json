{
  "name": "house-rules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "House Rules prototype: small mechanics that combine into short games.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test",
    "play": "tsx src/cli/play.ts",
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
